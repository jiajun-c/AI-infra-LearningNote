# 变量内存空间限定符

## 1. `__grid_constant__`

存储在参数空间(const bank)，launch时写入，生命周期是单次的launch，同时在每次launch之间是各自独立的，例如用在tma的描述符，提前加载到这个常量的内存中，避免每个线程自己单独拷贝一份

## 2. `__constant__`

存放在常量空间地址(64KB专用常量区)，需要通过host进行显式的拷贝，生命周期是进程级的。
