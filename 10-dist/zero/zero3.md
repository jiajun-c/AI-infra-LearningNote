# zero3


## 1. 定义

zero3在zero2的基础上继续切分了模型的参数

## 2. 实现模式

### 2.1 前向

前向的过程中需要进行一次AllGather来gather本层的完整参数

通信量为 $\frac{2S(N-1)}{N}$

### 2.2 反向

对于反向的过程对于梯度也是需要进行RS，同时对于参数也需要进行reduceScatter，全部通信量为  $\frac{4S(N-1)}{N}$

完整的通信量为 $\frac{6S(N-1)}{N}$

